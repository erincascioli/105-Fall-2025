namespace CompoundConditionals_Demo
{
    internal class Program
    {
        static void Main(string[] args)
        {


            /*
            // Give me a number in range:
            // Ask user for specific number
            // Retrieve that number
            // Parse to intended data type (int or double)
            // Number out of range?
            //      Provide feedback out of range
            // Number in range?
            //      Use that value for whatever

            Console.WriteLine("Enter a number between 5 (inclusive) and 20 (inclusive)");
            //string userNumber = Console.ReadLine()!;
            //int userNumberAsInt = int.Parse(userNumber);
            int userNumber = int.Parse(Console.ReadLine()!);

            // Out of range
            if(userNumber > 20 || userNumber < 5)
            {
                Console.WriteLine("Number out of range");
            }
            else
            {
                Console.WriteLine(userNumber * 10);
            }

            // IN range
            if (userNumber >= 5 && userNumber <= 20)
            {
                Console.WriteLine(userNumber * 10);
            }
            else
            {
                Console.WriteLine("Number out of range");
            }
            */




            // Player equipped with a weapon:
            // Yes: Continue on!
            // No:  End the program, demonstrate return;

            // Level 4 or less:
            // Not high enough --> Cannot enter the castle

            // At least level 5:  
            // High enough level --> Can enter the castle!
            //      Level is 5 or 6, boss is 50% strength
            //      Level is 7 - 9, boss is 100% strength
            //      Level is 10+, boss is 150% strength

        }
    }
}
